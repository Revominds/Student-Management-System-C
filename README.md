# Student-DB-Management-System
A Student Management System in C that allows users to add, view, search, and delete student records via a console interface. Records are stored in a readable text file (StudentDB.txt) with roll number, name, class, address, and percentage. Includes input validation, a menu system, and spinner animation for saving records.
